import os

bench_name='celebs_new'

os.mkdir(bench_name)
os.mkdir(bench_name + "/samples")
os.mkdir(bench_name + "/checkpoints")
os.mkdir(bench_name + "/checkpoints/GD")
os.mkdir(bench_name + "/checkpoints/G_inv")
